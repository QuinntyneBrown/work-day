import { Injectable } from "@angular/core";
import { Http } from "@angular/http";
import { TimeOff } from "./timeOff.model";
import { Observable } from "rxjs";


@Injectable()
export class TimeOffService {
    constructor(private _http: Http) { }

    public add(entity: TimeOff) {
        return this._http
            .post(`${this._baseUrl}/api/timeoff/add`, entity)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public get() {
        return this._http
            .get(`${this._baseUrl}/api/timeoff/get`)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public getById(options: { id: number }) {
        return this._http
            .get(`${this._baseUrl}/api/timeoff/getById?id=${options.id}`)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public remove(options: { id: number }) {
        return this._http
            .delete(`${this._baseUrl}/api/timeoff/remove?id=${options.id}`)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public get _baseUrl() { return ""; }

}
